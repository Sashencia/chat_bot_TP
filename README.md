# 🎭 Style-Switch Chatbot

Классический чат-бот с поддержкой диалоговой памяти и переключением стиля общения (формальный / неформальный).  
Полностью соответствует требованиям финального проекта по LangChain.

## ✅ Реализованные компоненты LangChain
| Компонент | Где используется |
|---------|----------------|
| `LLM` | `ChatGoogleGenerativeAI(model="gemini-2.5-flash-lite")` |
| `Memory` | `ConversationBufferMemory` (на пользователя) |
| `PromptTemplate` | Динамический системный промпт с подстановкой стиля |
| `Chain` | `prompt | llm | parser` → `RunnableSequence` |
| `OutputParser` | `StrOutputParser` + кастомная пост-обработка |
| `Tools` | `summarize_last_messages`, `detect_current_style`, `explain_style` |
| `Agent` | ReAct-агент (`create_react_agent`) с ограничением на инструменты |
| `Retrieval` | Векторный поиск по `style_guide.md` через FAISS |
| `DocumentLoader` | `TextLoader` для загрузки гайда по стилям |

## ▶️ Запуск
1. Создайте виртуальное окружение:
   ```bash
   python -m venv venv && source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows